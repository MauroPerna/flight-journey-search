version: "3.9"
services:
  # api:
  #   build: .
  #   env_file: .env
  #   depends_on: [db, redis]
  #   ports: ["8000:8000"]
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: trading
    ports: ["5432:5432"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
